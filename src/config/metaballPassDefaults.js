/** Default MetaballPass options when enabling the pass from the editor without a preset block. */
export const METABALL_PASS_DEFAULTS = {
  width: 1334,
  height: 750,
  resolutionScale: 0.5,
  blurStrength: 18,
  threshold: 0.32,
  edgeSoftness: 0.1,
};
